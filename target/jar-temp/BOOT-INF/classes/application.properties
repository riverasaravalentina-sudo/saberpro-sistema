# ===============================
# CONFIGURACIÓN SABER PRO - MYSQL
# ===============================

# Información de la aplicación
spring.application.name=Sistema SABER PRO
server.port=8080

# Configuración de Base de Datos MySQL - RAILWAY
spring.datasource.url=jdbc:mysql://mainline.proxy.rlwy.net:39252/railway?useSSL=true&requireSSL=false&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=kqFFGMQFAwNEVuPQEZIHziUesleuSQII

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console DESHABILITADA (usando MySQL)
spring.h2.console.enabled=false

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8

# Logging Configuration
logging.level.com.saberpro=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Security Configuration (Los usuarios están configurados en SecurityConfig.java)
# admin / admin123 (COORDINACION)
# coordinador / admin123 (COORDINACION)
# estudiante1 / estudiante123 (ESTUDIANTE)

# Configuración de archivos estáticos
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.cachecontrol.max-age=3600

# Configuración de validación
spring.jpa.properties.hibernate.validator.apply_to_ddl=false

# Configuración de inicialización de datos
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true